# 傅里叶光学笔记：普通相位偏移 与 光学相位 ψ(x)
## 1. 基础概念回顾
### 1.1 普通三角函数中的常数相位偏移 $\phi$
函数表达式：
$$y=A\cos(\omega x-\phi)$$
1. $\phi$：常数，与空间坐标 $x$ 无关，属于外加的固定相位偏移量。
2. 物理含义：将一整条标准余弦波形整体进行左右平移。
3. 最大值位置：严格出现在 $x=\phi$（MIT给出的相位偏移定义）。
4. 函数结构：单层函数，自变量仅有空间位置 $x$。

> 示例：$y=\cos(x‑2)$，波形最大值固定在 $x=2$，也就是原图中绿色虚线所在位置。

### 1.2 欧拉公式（复数光场的理论依据）
所有光波均为复数光场，电场复振幅表达式：
$$E(x)=A e^{i\psi(x)}=A\cos\big(\psi(x)\big)+i\cdot A\sin\big(\psi(x)\big)$$
- $\cos(\cdot)$：复电场的实部
- $\sin(\cdot)$：复电场的虚部

> 重要说明：外层的 cos、sin 仅用来完成复数分解，本身不代表相位偏移。

---

## 2. 核心重点：光学相位 $\psi(x)$
### 2.1 定义
光学相位 $\psi(x)$ 不再是固定常数，**它本身就是空间位置 $x$ 的函数**，即 $\psi=\psi(x)$。

在本篇笔记的相位波纹案例中，人为施加正弦分布形式的相位：
$$\psi(x)=\sin(x+\phi)$$

### 2.2 物理含义
平行光入射到光瞳平面（例如空间光调制器DM）时：
光瞳上每一个横向空间位置 $x$，都会对穿过该位置的光波引入大小不同的相位延迟。

$\psi(x)$ 的物理定义：**空间中不同位置，施加给光波的相位滞后量**。

### 2.3 双层嵌套结构（最大值位置偏移问题的根源）
将光学相位代入电场公式：
$$E(x)=A\cos\big(\sin(x+\phi)\big)+iA\sin\big(\sin(x+\phi)\big)$$

1. **内层（光学相位）**
$$\psi(x)=\sin(x+\phi)$$
真正的光学相位项，代表DM光瞳平面上随空间位置分布的相位延迟；$\phi$ 是施加在光学相位上的整体平移偏移量。

2. **外层（复数分解项）**
仅为 $\cos(\cdot)$、$\sin(\cdot)$，由欧拉恒等式推导而来，仅用于拆分复数的实部、虚部，和相位平移无关。

> 关键对比
> - 普通余弦（单层函数）：$y=\cos(x-\phi)$，最大值位于 $x=\phi$；
> - 光学光场（嵌套复合函数）：$\cos\big(\sin(x+\phi)\big)$。
> 波形已经不再是标准的余弦曲线，因此最大值不会出现在 $x=\phi$（绿色虚线处），这也是原笔记开篇疑问的来源。

### 2.4 外加偏移量 $\phi$ 的两种物理作用
1. **平面内横向平移**：$\psi(x)=\sin(x+\phi)$，DM器件上的正弦相位波纹，在平面内左右移动。
2. **纵向传播相位延迟**：整体相位偏移，使光束沿光的传播方向（$z$轴）产生相位滞后，平面内图案不发生平移。

### 2.5 工程应用（傅里叶光学）
在DM上加载正弦相位波纹 $\psi(x)=\sin(x)$；
经过透镜完成傅里叶变换后，会在焦平面（像平面）生成单个亮斑。
该原理是光束偏转、光场调制的理论基础。

---

## 3. 二者对比表格
| 分类 | 普通常数相位偏移 $\phi$ | 光学相位 $\psi(x)$ |
| ---- | ---- | ---- |
| 本质 | 固定常数，与空间坐标$x$无关 | 空间坐标$x$的函数，$\psi=\psi(x)$ |
| 函数形式 | 单层标准三角函数 $y=\cos(x-\phi)$ | 嵌套复合函数 $\cos\big(\psi(x)\big)=\cos\big(\sin(x+\phi)\big)$ |
| 最大值位置 | 严格位于 $x=\phi$ | 不再位于$x=\phi$，波形发生畸变 |
| 物理意义 | 平移整条三角函数曲线 | 光瞳平面上，不同空间位置对光波产生的相位延迟 |
| 适用场景 | 基础一维信号分析 | 复数光场、空间光调制、傅里叶成像 |

---

## 4. 总结
1. 普通相位偏移 $\phi$：单纯的常数，作用是平移标准正弦、余弦波形；
2. 光学相位 $\psi(x)$：由空间位置决定的相位延迟函数。整体光场属于复合嵌套函数，波形不再是标准余弦，峰值不会出现在预设的偏移位置。

## 5. 本篇笔记目的
区分普通三角函数相位，以及光瞳平面上和空间位置相关的光学相位 $\psi(x)$；厘清复电场内正弦、余弦的分工，为后续的光瞳调制、傅里叶成像分析打下数学基础。
